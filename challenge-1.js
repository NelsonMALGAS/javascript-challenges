/**
 * Instruction:
 * Implement the handleColorChange function logic so that the background color
 * of the document's body changes to the color passed as an argument to the function.
 *
 * Hint:
 * Use the style property of the document.body object to set the backgroundColor.
 * The value of the color parameter passed to the function should determine the background color.
 *
 * For example, if handleColorChange("blue") is called, the body background should turn blue.
 */


const btn = document.querySelector("#btn");

const handleColorChange = (color) =>{}

btn.addEventListener("click" , () => handleColorChange("blue"))
